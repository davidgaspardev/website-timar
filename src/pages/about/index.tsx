import { Header } from "../../components";

export default function AboutPage(): JSX.Element {

    return (
        <div>
            <Header />
            <h1>About Page</h1>
        </div>
    );
}