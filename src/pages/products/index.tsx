import { Header } from "../../components";

export default function ProductsPage(): JSX.Element {

    return (
        <div>
            <Header id="header-main" />
            <h1>Products Page</h1>
        </div>
    );
}